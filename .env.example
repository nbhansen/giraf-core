# GIRAF Core API — Environment Variables
# Copy to .env and fill in values

# === Required ===
DJANGO_SECRET_KEY=change-me-to-a-random-secret-key
JWT_SECRET=shared-secret-with-app-backends

# === Database (PostgreSQL) ===
POSTGRES_DB=giraf_core
POSTGRES_USER=giraf
POSTGRES_PASSWORD=giraf
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# === Django ===
DJANGO_SETTINGS_MODULE=config.settings.dev

# === Production only ===
# ALLOWED_HOSTS=api.giraf.example.com
# CORS_ALLOWED_ORIGINS=https://app.giraf.example.com
